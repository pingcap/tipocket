metadata:
  name: tipocket-txn-rand-pessimistic
  namespace: argo
spec:
  templates:
    - name: tipocket-txn-rand-pessimistic
      inputs:
        parameters:
          - name: image_version
      container:
        name: tipocket
        image: 'pingcap/tipocket:latest'
        command:
          - sh
          - '-c'
          - |
            /bin/txn-rand-pessimistic \
            -namespace=tipocket-txn-rand-test-1
            -namespace=tipocket-txn-rand-pessimistic-test \
            -hub=docker.io \
            -storage-class=local-scsi \
            -purge=true \
            -image-version={{inputs.parameters.image_version}} \
            -nemesis=random_kill,short_kill_tikv_1node,short_kill_pd_leader,kill_tikv_1node_5min \
            -run-time=8h
      retryStrategy:
        limit: 0
        retryPolicy: Always